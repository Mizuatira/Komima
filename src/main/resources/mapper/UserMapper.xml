<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.komima.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.komima.entity.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="role" property="role"/>
        <result column="nickname" property="nickname"/>
        <result column="gender" property="gender"/>
        <result column="college" property="college"/>
        <result column="major" property="major"/>
        <result column="phone" property="phone"/>
        <result column="wechat" property="wechat"/>
        <result column="qq" property="qq"/>
        <result column="email" property="email"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="com.komima.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, role, nickname, create_time)
        VALUES (#{username}, #{password}, #{role}, #{nickname}, #{createTime})
    </insert>

    <select id="selectById" resultMap="UserResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <select id="selectByUsernameAndPassword" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password}
    </select>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT * FROM user ORDER BY create_time DESC
    </select>

    <update id="update" parameterType="com.komima.entity.User">
        UPDATE user
        SET username = #{username}, password = #{password}, role = #{role},
            nickname = #{nickname}, gender = #{gender}, college = #{college},
            major = #{major}, phone = #{phone}, wechat = #{wechat},
            qq = #{qq}, email = #{email}, create_time = #{createTime}
        WHERE id = #{id}
    </update>

    <update id="updateProfile" parameterType="com.komima.entity.User">
        UPDATE user
        SET nickname = #{nickname}, gender = #{gender},
            college = #{college}, major = #{major},
            phone = #{phone}, wechat = #{wechat},
            qq = #{qq}, email = #{email}
        WHERE id = #{id}
    </update>

    <update id="updateRole">
        UPDATE user SET role = #{role} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
</mapper>
